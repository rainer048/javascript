var question = prompt ('Каково официальное название JavaScript, ну если вдруг вы догадываетесь' , '');

  if (question == 'ЕСМАScript' || 'ECMAScript'){
    alert ('Красава');
  }
  else {
    alert('Ничего, это в сущности и не важно, но по секрету, это ECMAScript');
 }
